[project]
name = "neuroclient-workspace"
version = "0.0.0"
description = "Developer tooling for the NeuroClient Rust workspace"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pre-commit>=3.7.0"]
